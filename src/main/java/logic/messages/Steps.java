package logic.messages;

public enum Steps {
    START,
    AFTER_CLIENT_ID,
    AFTER_CLIENT_NAME,
    AFTER_CLIENT_LEVEL,
    AFTER_CLIENT_ROUTE,
    START_GAME,
    DURING_GAME,
    AFTER_FINISH
}
